Feature: <%= feature.title %><br/>
<%= link_to_remote "Expand", :update => dom_id(feature), :url => package_feature_path(feature.package, feature), :method => :get %>&nbsp;
<%= render :partial => "features/show_common", :locals => { :feature => feature } %>
