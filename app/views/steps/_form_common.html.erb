<%= form.text_field_for(:text, {}, { :url => url_for(:controller => :steps, :action => :auto_complete_text),
                                     :extra_params => { :scenario_id => model.scenario.id,
                                                        :position => "function() { return UnorderedList.elementPosition($('##{form.id_for(:text)}').closest('li')) }" } }) %>
