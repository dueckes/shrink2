<%= form.text_field_for(:text, {}, { :autocomplete_url => url_for(:controller => :steps, :action => :auto_complete_for_step_text),
                                     :extra_params => { :scenario_id => model.scenario.id,
                                                        :position => "function() { return new AddAnywhereForm($('##{form.id_for(:text)}').closest('li')).positionInList() }" } }) %>
<%= form.commit_button(:style => "display: none") %>
