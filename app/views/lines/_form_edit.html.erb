<%= render(:partial => "common/form_edit", :locals => { :model_name => :line,
                                                        :model => line,
                                                        :url => package_feature_line_path(line.package, line.feature, line),
                                                        :form_builder => FeatureLineFormBuilder }) %>
