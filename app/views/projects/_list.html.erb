<ul id="projects">
  <% projects.each do |project| %>
    <%= li_for(project, :style => element_style(:hidden => project == hide)) do -%>
      <%= link_to project.name, project_path(project), :remote => true, :method => :get, :id => dom_id(project, :show_link) %>
    <% end -%>
  <% end -%>
</ul>
