<%= cell_tag(cell_presenter) do -%>
  <%= link_to with_default_text(cell_presenter.text), cell_presenter.table_presenter.edit_url,
              :remote => true, :method => :get,
              :id => "#{cell_presenter.dom_id}_edit_link", :class => :editable_text_link, :title => "Click to Edit Table" %>
<% end -%>
