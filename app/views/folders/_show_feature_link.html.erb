<li id="<%= dom_id(feature) %>_folder_link_area">
  <%= link_to_remote feature.title, { :url => { :controller => :features, :action => :show_detail, :id => feature.id } }, { :id => dom_id(feature, :folder_link), :title => "View Feature" } %>
</li>
