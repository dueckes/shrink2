folder_area = render_folder_to_string(:folder => @root_folder, :highlighted_feature => @feature)
page << %{
  $('#folders_area').fadeOutAndIn(function(object) {
    FoldMenuItemFactory.createFromName('folders').show();
    object.html(#{::ActiveSupport::JSON.encode(folder_area)})
  })
}
