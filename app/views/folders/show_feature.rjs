folders_area = render("folders/show_root", :root_folder => @root_folder, :highlighted_feature => @feature)
page << %{
  $('#folders_area').fadeOutAndIn(function(object) {
    FoldMenuItemFactory.createFromName('folders').show();
    object.html(#{::ActiveSupport::JSON.encode(folders_area)})
  })
}
