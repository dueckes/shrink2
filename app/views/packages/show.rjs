page.replace("#{dom_id(@package)}_expand_link", link_to_remote("Shrink", :id => "#{dom_id(@package)}_shrink_link", :update => "#{dom_id(@package)}_children", :url => { :action => :shrink, :id => @package.id }, :method => :get))
page.replace_html("#{dom_id(@package)}_children", :partial => "list", :locals => { :packages => @package.children })
page.visual_effect(:appear, "#{dom_id(@package)}_children")
