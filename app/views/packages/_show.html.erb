<%= package.name %>&nbsp;
<%= link_to_remote "Expand", :update => "#{dom_id(package)}_children", :url => package_path(package), :method => :get %>&nbsp;
<%= link_to "Features", :url => package_features_path(package), :method => :get %>&nbsp;
<%= link_to_remote "Edit", :update => dom_id(package), :url => edit_package_path(feature), :method => :get %>&nbsp;
<%= link_to_remote "Delete", :confirm => "Are you sure you want to delete this package?", :loading => visual_effect(:fade, dom_id(package)), :url => package_path(package), :method => :delete %>&nbsp;
<div id="<%= dom_id(package) %>_children">
</div>
<br/>
<br/>
